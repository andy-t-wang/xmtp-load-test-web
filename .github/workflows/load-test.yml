name: XMTP Load Test

on:
  workflow_dispatch:
    inputs:
      inbox_id:
        description: "Inbox ID to add to groups"
        required: true
        type: string
      network:
        description: "Network to use"
        required: true
        type: choice
        options:
          - dev
          - local
          - staging
          - production
        default: "dev"
      duration:
        description: "Test duration in seconds"
        required: true
        type: string
        default: "30"
      num_groups:
        description: "Number of groups to create"
        required: true
        type: string
        default: "10"
      num_dms:
        description: "Number of DMs (2-person conversations) to create"
        required: false
        type: string
        default: "0"
      interval:
        description: "Seconds between message batches"
        required: true
        type: string
        default: "1"
      messages_per_batch:
        description: "Messages per group per batch"
        required: true
        type: string
        default: "3"
      test_id:
        description: "Unique test identifier"
        required: true
        type: string
      group_size:
        description: "Number of members in each group"
        required: false
        type: string
        default: "10"

jobs:
  load-test:
    name: "Test ${{ inputs.test_id }} | Network: ${{ inputs.network }} | Groups: ${{ inputs.num_groups }} | Inbox: ${{ inputs.inbox_id }}"
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Print Test Info
        run: |
          echo "🧪 Starting XMTP Load Test"
          echo "Test ID: ${{ inputs.test_id }}"
          echo "Network: ${{ inputs.network }}"
          echo "Duration: ${{ inputs.duration }}s"
          echo "Groups: ${{ inputs.num_groups }}"
          echo "DMs: ${{ inputs.num_dms }}"
          echo "Group Size: ${{ inputs.group_size }}"
          echo "Inbox ID: ${{ inputs.inbox_id }}"

      - name: Checkout this repo
        uses: actions/checkout@v4

      - name: Cache libxmtp clone
        uses: actions/cache@v4
        with:
          path: libxmtp
          key: libxmtp-andy-repo-v7-refresh-${{ hashFiles('.github/workflows/load-test.yml') }}
          restore-keys: |
            libxmtp-andy-repo-v7-refresh-

      - name: Clone libxmtp (if not cached)
        run: |
          if [ ! -d "libxmtp" ]; then
            echo "🔄 Cloning libxmtp repository..."
            git clone --depth 1 https://github.com/andy-t-wang/libxmtp.git libxmtp
          else
            echo "✅ Updating cached libxmtp repository"
            cd libxmtp
            # Force fetch latest changes
            git fetch --depth 1 origin main
            git reset --hard origin/main
            cd ..
          fi
          cd libxmtp
          echo "LIBXMTP_PATH=$(pwd)" >> $GITHUB_ENV

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y jq bc

      - name: Setup Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          profile: minimal
          override: true

      - name: Cache Rust toolchain and registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ~/.rustup
          key: ${{ runner.os }}-rust-toolchain-v2-clean-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-rust-toolchain-v2-clean-

      - name: Cache libxmtp build
        uses: actions/cache@v4
        with:
          path: libxmtp/target
          key: ${{ runner.os }}-libxmtp-andy-build-v8-refresh-${{ hashFiles('libxmtp/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-libxmtp-andy-build-v8-refresh-

      - name: Cache xdbg binary
        uses: actions/cache@v4
        with:
          path: libxmtp/target/release/xdbg
          # Stable key that includes group naming and DM sync functionality
          key: ${{ runner.os }}-xdbg-andy-binary-v8-refresh
          restore-keys: |
            ${{ runner.os }}-xdbg-andy-binary-v8-refresh-

      - name: Check if xdbg already built
        id: check_xdbg
        run: |
          cd libxmtp
          if [ -f "target/release/xdbg" ]; then
            echo "✅ xdbg binary already exists with group naming and DM sync support"
            echo "xdbg_exists=true" >> $GITHUB_OUTPUT
          else
            echo "🔨 xdbg binary not found, will build"
            echo "xdbg_exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Build xdbg (if needed)
        if: steps.check_xdbg.outputs.xdbg_exists != 'true'
        run: |
          cd libxmtp
          echo "🔨 Building xdbg with optimized settings..."

          # Use all available CPU cores for faster compilation
          export CARGO_BUILD_JOBS=$(nproc)

          # Enable incremental compilation for faster rebuilds
          export CARGO_INCREMENTAL=1

          # Build with optimized flags for faster compilation
          cargo build --release --bin xdbg \
            --config 'profile.release.lto="thin"' \
            --config 'profile.release.codegen-units=1' \
            --config 'profile.release.panic="abort"' \
            -j $(nproc)

          # Verify the binary was built successfully
          if [ -f "target/release/xdbg" ]; then
            echo "✅ xdbg binary built successfully"
            ls -la target/release/xdbg
          else
            echo "❌ Failed to build xdbg binary"
            exit 1
          fi

      - name: Run Load Test
        id: load_test
        env:
          INBOX_ID: ${{ inputs.inbox_id }}
          NETWORK: ${{ inputs.network }}
          DURATION: ${{ inputs.duration }}
          NUM_GROUPS: ${{ inputs.num_groups }}
          INTERVAL: ${{ inputs.interval }}
          MESSAGES_PER_BATCH: ${{ inputs.messages_per_batch }}
          TEST_ID: ${{ inputs.test_id }}
          NUM_DMS: ${{ inputs.num_dms }}
          GROUP_SIZE: ${{ inputs.group_size }}
          XDBG_PATH: ${{ env.LIBXMTP_PATH }}/target/release/xdbg
        run: |
          chmod +x enhanced_load_test.sh
          ./enhanced_load_test.sh "$INBOX_ID" "$NETWORK" "$INTERVAL" "$DURATION" "$NUM_GROUPS" "$MESSAGES_PER_BATCH" "$TEST_ID" "$NUM_DMS" "$GROUP_SIZE"

      - name: Upload Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: load-test-results-${{ inputs.test_id }}
          path: |
            results_${{ inputs.test_id }}.json
            logs_${{ inputs.test_id }}.txt
          retention-days: 7

      - name: Comment on Results
        if: always()
        run: |
          echo "Load test completed for test ID: ${{ inputs.test_id }}"
          if [ -f "results_${{ inputs.test_id }}.json" ]; then
            echo "Results:"
            cat results_${{ inputs.test_id }}.json
          fi
